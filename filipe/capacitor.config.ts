import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.ranipassos',
  appName: 'Rani Passos',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
